# Directorio de inventario subido por FTP

Esta carpeta actua como destino remoto del servidor FTP local.

Cuando se ejecuta `upload_inventario.py`, el archivo `productos.csv` se transfiere aqui y despues es leido por `server_inventario.py`.
